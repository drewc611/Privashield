# PrivaShield local development defaults. Change credentials before non-development use.
PRIVASHIELD_ENVIRONMENT=development
PRIVASHIELD_LOG_LEVEL=INFO
PRIVASHIELD_ENFORCEMENT_MODE=observe
PRIVASHIELD_DASHBOARD_PORT=8080

# Authentication remains disabled for loopback-only development compatibility.
# Set to local before any non-loopback or multi-user deployment.
PRIVASHIELD_AUTH_MODE=disabled
# Bootstrap token is required to establish the first local administrator when local auth is enabled.
# Use a high-entropy secret and remove/rotate it after durable administrator principals exist.
# PRIVASHIELD_BOOTSTRAP_ADMIN_TOKEN=replace-with-a-high-entropy-secret
# Optional token used by Suricata/Zeek collectors and the file monitor when local auth is enabled.
# Create a dedicated operator principal and place its one-time token here through your secret workflow.
# PRIVASHIELD_API_TOKEN=psh_replace-with-issued-operator-token

# Local AI is opt-in because the model download can be several GB.
PRIVASHIELD_OLLAMA_ENABLED=false
PRIVASHIELD_OLLAMA_MODEL=gemma3

# Signed policy governance is disabled until an operator supplies the Ed25519 public key.
# The API only receives the base64-encoded raw public key. Keep the private key offline.
# PRIVASHIELD_POLICY_VERIFICATION_PUBLIC_KEY=base64-encoded-32-byte-ed25519-public-key
PRIVASHIELD_POLICY_VERIFICATION_KEY_ID=local-v1

# Linux host sensor profiles are opt-in. Set this to a capture interface before use.
PRIVASHIELD_NETWORK_INTERFACE=eth0
# PRIVASHIELD_MONITOR_PATH=/absolute/path/to/monitor

POSTGRES_DB=privashield
POSTGRES_USER=privashield
POSTGRES_PASSWORD=change-me-before-production
